import { fromEnumToArray } from "../utils";

export enum stack {
  apollographql,
  axios,
  css,
  django,
  dotenv,
  express,
  fastapi,
  git,
  graphql,
  html,
  javascript,
  jest,
  jsonwebtoken,
  mongodb,
  mongoose,
  nextjs,
  nodejs,
  passport,
  postgresql,
  python,
  rabbitmq,
  reactrouter,
  react,
  sequelize,
  socketio,
  styledcomponents,
  terminal,
  typescript,
  vscode,
  webpack,
}
export enum state {
  developing,
  testing,
  builded,
  deprecated,
}

export enum kind {
  test,
  production,
  development,
  component,
  project,
}
